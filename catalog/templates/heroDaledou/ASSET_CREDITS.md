# 资源说明

`raw/splash-source.png`、角色/敌人/武器 roster，以及商店封面均为本项目委托生成的原创图像；生成记录使用工作区规定的 `generate_image.py`。网页原型中同样由该脚本生成的英雄、武器、技能、伙伴和事件图也作为源图随本项目交付。`assets/*.xic` 和预览 PNG 由这些原图本地转换而来。

当前可见角色、敌人和武器均优先使用对应 XIC；按钮与进度条使用 `index.lua` 的 XTApp 0.8 基础绘制 API。素材均为本项目生成或其本地转换结果，本包没有复制网页原型中的第三方开放素材。
